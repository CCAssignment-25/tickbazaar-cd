apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bookings
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/CCAssignment-25/tickbazaar-cd.git'
    path: 'charts/bookings'
    targetRevision: HEAD
    helm:
      valueFiles:
      - values.yaml
      values: |
        image:
          tag: v1
        hpa:
          enabled: true
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
